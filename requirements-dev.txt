# Click-processor test-only dependencies (NOT installed in the edge
# runtime image). The click-processor is the public edge mirror
# (tutoroton/tds-click-processor, deployed via `git subtree push`) and
# must stay lean — pytest/fakeredis would bloat every edge node — so
# the test stack lives here, separate from requirements.txt. Mirrors
# the collector's requirements-dev.txt convention.
#
# Install for local/CI test runs:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.3.0    # @pytest.mark.asyncio in the unit suite
fakeredis==2.35.1        # in-memory Redis for dedup / cache paths (test-only)
ruff==0.11.13           # CI lint gate (see pr-validation.yml); pinned to match process-service
pyyaml>=5.1             # test_edge_node_stats_carveout_topology.py reads docker-compose.node.yml.
                        # Already present transitively (uvicorn[standard] declares pyyaml>=5.1,
                        # verified against the pinned 0.34.2) — declared anyway so dropping the
                        # `[standard]` extra fails at install time rather than as a surprise
                        # ImportError in a security guard. Test-only: the edge runtime image
                        # installs requirements.txt alone, so this does not reach a node.
